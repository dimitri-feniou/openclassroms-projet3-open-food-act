# Projet 3 Openclassrooms Préparez des données pour un organisme de santé publique
## Installation 
Installer les packages nécessaires au projet via le `requirements.txt` avec la commande :

``` bash
pip install -r requirements.txt
```
